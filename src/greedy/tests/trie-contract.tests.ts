import { expect } from 'chai';

describe('trie-contract', () => {
    it('Should fail until we have tests', () => {
        expect.fail('Missed requirements and tests for this implementation');
    });
});
